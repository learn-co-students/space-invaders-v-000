function CrewMember(position) {
  this.title = position;
}
CrewMember.prototype.currentShip() {
  if(Spaceship.crew = false) {
    (return "Looking for a Rig"); //if they aren\'t assigned to a ship',
  }else{
    return new Spaceship(name);
  }
}
CrewMember.prototype.engageWarpDrive() {
 return "had no effect"; //when the crew member tries to use their special ability
}
CrewMember.prototype.setsInvisibility() {
  return "had no effect"; //when the crew member tries to use their special ability
}
CrewMember.prototype.chargePhasers() {
  return "had no effect"; //when the crew member tries to use their special ability
}
